import { createVar } from "css-variable";

// eslint-disable-next-line import/no-anonymous-default-export
export default {
  primaryColor: createVar(),
  primaryColorLighten8: createVar(),
  primaryColorLighten92: createVar(),
  primaryColorLighten96: createVar(),
  secondaryColor: createVar(),
  accentColor: createVar(),
  lightBackgroundColor: createVar(),
  warningColor: createVar(),
  linkColor: createVar(),
  linkHoverColor: createVar(),
  white: createVar(),
  black: createVar(),
};
